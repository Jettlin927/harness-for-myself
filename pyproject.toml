[project]
name = "minimal-agent-harness"
version = "0.1.0"
description = "Minimal single-agent harness with reliability guardrails."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]
